package in.r.util.java8;

public class SumUsingFilterJava8 {
  public static void main(String[] args) {
    //        debitsL =
    //                sortedPostings
    //                        .stream()
    //                        .filter(posting -> posting.getLedgerRecordType() ==
    // LedgerRecordType.DEBIT)
    //                        .mapToLong(TransactionV2.Posting::getAmount)
    //                        .sum();
    //        creditsL =
    //                sortedPostings
    //                        .stream()
    //                        .filter(posting -> posting.getLedgerRecordType() ==
    // LedgerRecordType.CREDIT)
    //                        .mapToLong(TransactionV2.Posting::getAmount)
    //                        .sum();

  }
}
